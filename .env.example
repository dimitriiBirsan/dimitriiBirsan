# Domain configuration
DOMAIN=dimitrii.dev

# Traefik certresolver name configured in your Traefik setup (e.g. letsencrypt, myresolver)
CERT_RESOLVER=letsencrypt

# Name of the external docker network shared with Traefik (e.g. traefik-net, proxy, web)
TRAEFIK_NETWORK=traefik-net
